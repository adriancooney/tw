{
  "name": "tw-beta",
  "version": "0.1.1",
  "description": "Teamwork Projects CLI and git integration.",
  "main": "build/src/TeamworkAPI.js",
  "bin": {
    "tw": "./build/bin/tw"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "test": "mocha --compilers js:babel/register",
    "bin-tool": "sed -i.bak s/babel-node\\ --/node/g ./build/bin/* && rm build/bin/*.bak && rename build/bin/*.js --delete=\".js\" && chmod +x build/bin/* && cp ./package.json build/package.json",
    "build": "mkdir -p ./build/bin && ./node_modules/.bin/babel ./bin/* -d ./build && mkdir build/src && ./node_modules/.bin/babel ./src -d ./build/src && npm run bin-tool",
    "parser": "pegjs --allowed-start-rules duration,task,progress,commit,installation src/parser/TeamworkParser.pegjs",
    "prepublish": "npm run build"
  },
  "keywords": [
    "tw"
  ],
  "author": "Adrian Cooney <adrian.cooney@teamwork.com>",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.6.14",
    "chalk": "^1.0.0",
    "commander": "^2.8.1",
    "editor": "^1.0.0",
    "inquirer": "^0.8.5",
    "open": "0.0.5",
    "os-tmpdir": "^1.0.1",
    "rc": "^1.0.3"
  },
  "dependencies": {
    "bluebird": "^2.9.30",
    "debug": "^2.2.0",
    "moment": "^2.10.3",
    "request": "^2.58.0"
  }
}
