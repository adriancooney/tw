import Models from "../src/model";
import assert from "assert";

describe("Models", () => {
    describe("serialization", () => {
        // Models must follow a serialization
        // rule of model === new Model(model.toJSON())
        it("should be serialized correctly.");
    });
});